import { CollectionDashboard } from "@/components/app/CollectionDashboard";

export const metadata = {
  title: "My collection · Cardfolio",
};

export default function CollectionPage() {
  return <CollectionDashboard />;
}
